services:
  postgres:
    image: postgres:16
    container_name: yti-postgres
    environment:
      POSTGRES_DB: yti
      POSTGRES_USER: yti
      POSTGRES_PASSWORD: yti
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: